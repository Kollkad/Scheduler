[pytest]
# путь до тестов
testpaths = tests
pythonpath = ../backend

markers =
    slow: медленные тесты (интеграционные, с реальными данными)
    unit: быстрые юнит-тесты
    ci: тесты для CI

# Настройки вывода
addopts =
    -v
    --tb=short
    --strict-markers
    --timeout=120

