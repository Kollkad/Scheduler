# Архитектура фронтенд модулей проекта

## Общая информация
- **Технологический стек:** TypeScript + React
- **Архитектура:** Модульная компонентная архитектура с разделением на страницы, компоненты и утилиты
- **Состояние:** Управление состоянием через React Context и локальный state

## Навигация по модулям
- [📱 `pages`](#модуль-pages) - основные страницы приложения
- [⚙️ `config`](#модуль-config) - конфигурационные параметры системы
- [🔄 `contexts`](#модуль-contexts) - управление глобальным состоянием
- [🎣 `hooks`](#модуль-hooks) - кастомные React хуки
- [🛠️ `utils`](#модуль-utils) - вспомогательные утилиты и функции
- [🔌 `services`](#модуль-services) - слой взаимодействия с API
- [🧩 `components`](#модуль-components) - переиспользуемые UI компоненты

---

## Модуль `pages`

### Назначение
Основные страницы приложения, представляющие собой контейнеры для конкретных функциональных разделов системы. Каждая страница объединяет компоненты, логику и API вызовы для решения конкретных бизнес-задач.

### Внутренняя структура
```
frontend/client/pages/
├── NotFound.tsx                    # Страница 404 ошибки
├── Tasks.tsx                       # Управление задачами сотрудников
├── DocumentDetail.tsx              # Детали документа
├── Search.tsx                      # Поиск (заглушка)
├── SystemOverview.tsx              # Обзор системы с инструкциями
├── Rainbow.tsx                     # Цветовая классификация дел
├── TermsOfSupport.tsx              # Анализ сроков сопровождения
├── FilteredCases.tsx               # Фильтрованные дела
├── TaskDetail.tsx                  # Детали задачи
└── DynamicCaseDetail.tsx           # Динамические детали дела
```

### Основные компоненты

**NotFound.tsx**
- Страница 404 ошибки
- Навигация на главную страницу
- Минималистичный UI с кнопкой возврата

**Tasks.tsx**
- Поиск задач по ответственному исполнителю
- Фильтрация через SettingsForm
- Табличное представление задач с навигацией к деталям
- Интеграция с API задач

**DocumentDetail.tsx**
- Детальное отображение информации о документе
- Группировка полей по категориям (общие, даты, статусы)
- Вкладки для навигации по данным документа
- Интеграция с CaseService

**SystemOverview.tsx**
- Инструкция по работе с системой
- Описание цветовой системы Rainbow
- Детализация этапов сопровождения дел
- Интерактивные аккордеоны с проверками

**Rainbow.tsx**
- Визуализация цветовой классификации дел
- Диаграмма распределения по цветам
- Фильтрация и формирование отчетов
- Навигация к фильтрованным делам

**TermsOfSupport.tsx**
- Анализ сроков сопровождения дел
- Переключение между исковым и приказным производством
- Сегментированные диаграммы этапов
- Мониторинг документов

**FilteredCases.tsx**
- Отображение отфильтрованных дел
- Поддержка фильтров из Rainbow, Terms и Documents
- Адаптивные колонки таблицы в зависимости от источника
- Навигация к деталям дела/документа

**TaskDetail.tsx**
- Детальная информация о задаче
- Статусы выполнения и мониторинга
- Навигация к источнику задачи (дело/документ)
- Структурированное представление данных

**DynamicCaseDetail.tsx**
- Динамическое отображение деталей дела
- Группировка полей по категориям
- Вкладки для навигации по данным
- Интеграция с CaseService

### Зависимости
- **Зависит от:** `components`, `services`, `hooks`, `contexts`, `config`
- **Используется:** React Router для маршрутизации

### Особенности
- Единая структура PageContainer для консистентности
- Интеграция с AnalysisContext для управления состоянием
- Поддержка загрузки состояний и обработки ошибок
- Навигация между связанными сущностями (дела↔документы↔задачи)
- Адаптивные таблицы с конфигурируемыми колонками
- Модульная архитектура с переиспользуемыми компонентами

**[⬆️ вернуться к модулям](#навигация-по-модулям)**

---


## Модуль `config`

### Назначение
Централизованное хранилище конфигурационных параметров системы. Содержит настройки визуализации, таблиц, функциональных флагов и конфигурации фильтров для обеспечения единообразия интерфейса.

### Внутренняя структура
```
frontend/client/config/
├── chartConfig.ts           # Конфигурация диаграмм и графиков
├── featureFlags.ts          # Функциональные флаги системы
├── sorterConfig.ts          # Настройки фильтрации и сортировки
└── tableConfig.ts           # Конфигурации таблиц и преобразований данных
```

### Основные компоненты

**chartConfig.ts**
- Конфигурации цветовых схем для всех типов диаграмм
- Настройки сегментов для Rainbow, искового и приказного производства
- Маппинг технических имен на читаемые русские названия
- Базовые сегменты статусов (в срок/просрочено/нет данных)

**featureFlags.ts**
- Управление доступностью функциональности
- Флаг `hasPreviousReport` - поддержка сравнения периодов
- Флаг `enableComparison` - включение модуля сравнения

**sorterConfig.ts**
- Конфигурация полей фильтрации для разных страниц
- Настройки поиска и автодополнения
- Интеграция с API endpoints для загрузки опций

**tableConfig.ts**
- Конфигурации колонок для всех типов таблиц
- Функции преобразования данных из бэкенд формата
- Маппинг статусов и этапов на русские названия
- Базовые стили оформления таблиц

### Зависимости
- **Зависит от:** Типы компонентов (TableTypes, SorterTypes)
- **Используется:** Всеми компонентами страниц для консистентного отображения

### Особенности
- Единый источник истины для всех визуальных настроек
- Поддержка функциональных флагов для постепенного rollout
- Типизированные конфигурации для TypeScript
- Модульная структура с переиспользуемыми сегментами
- Автоматическое преобразование данных бэкенда в фронтенд формат
- Локализация технических имен в читаемые русские названия

**[⬆️ вернуться к модулям](#навигация-по-модулям)**

---

## Модуль `contexts`

### Назначение
Управление глобальным состоянием приложения через React Context. Централизованное хранение результатов анализа, статусов выполнения и состояния данных между компонентами.

### Внутренняя структура
```
frontend/client/contexts/
└── AnalysisContext.tsx    # Контекст управления анализом данных
```

### Основные компоненты

**AnalysisContext.tsx**
- Управление состоянием выполнения анализа (прогресс, ошибки)
- Хранение результатов всех модулей анализа (Rainbow, Terms, Documents, Tasks)
- Управление статусом загруженных файлов
- Методы запуска, отмены и очистки анализа
- Интеграция с локальным хранилищем для сохранения результатов

### Ключевые функции

**Управление анализом:**
- `runAnalysis()` - полный последовательный анализ всех модулей
- `runSingleAnalysis()` - анализ конкретного модуля
- `cancelAnalysis()` - отмена текущего анализа
- `clearResults()` - очистка всех результатов

**Состояние данных:**
- Хранение результатов Rainbow, Terms V2, Documents, Tasks
- Отслеживание прогресса выполнения с весами
- Управление статусом загруженных файлов
- Восстановление из localStorage при инициализации

**Интеграции:**
- Подписка на прогресс через analysisService
- Автоматическое сохранение в localStorage
- Обновление статуса файлов через API

### Зависимости
- **Зависит от:** `services/analysis`, `services/api/client`, `config/featureFlags`
- **Используется:** Всеми страницами для доступа к данным анализа

### Особенности
- Последовательное выполнение анализов с приоритетами
- Весовая система прогресса для точного отображения
- Автоматическое восстановление состояния при перезагрузке
- Обработка ошибок с сохранением частичных результатов
- Интеграция с функциональными флагами
- Отписка от событий при размонтировании

**[⬆️ вернуться к модулям](#навигация-по-модулям)**

---

## Модуль `hooks`

### Назначение
Кастомные React хуки для извлечения и управления состоянием приложения. Инкапсулируют сложную логику работы с данными, предоставляя простые интерфейсы для компонентов.

### Внутренняя структура
```
frontend/client/hooks/
├── useCaseSearch.ts        # Поиск дел по кодам
└── useFilterOptions.ts     # Управление опциями фильтров
```

### Основные компоненты

**useCaseSearch.ts**
- Поиск дел по коду из всех доступных источников данных
- Агрегация кодов дел из Rainbow, Documents, Terms модулей
- Фильтрация с частичным совпадением и ограничением результатов
- Мемоизация для оптимизации производительности

**useFilterOptions.ts**
- Загрузка опций фильтрации для полей формы
- Управление состоянием загрузки и ошибок
- Кэширование полученных опций
- Интеграция с FilterService для получения данных

### Ключевые функции

**useCaseSearch:**
- `searchTerm` - текущий поисковый запрос
- `searchResults` - отфильтрованные коды дел
- `hasResults` - флаг наличия результатов
- Автоматическое обновление при изменении данных анализа

**useFilterOptions:**
- `loadOptions()` - загрузка опций для указанных колонок
- `getOptionsForField()` - получение опций для конкретного поля
- Управление состояниями `loading` и `error`
- Интеграция с системой фильтрации

### Зависимости
- **Зависит от:** `contexts/AnalysisContext`, `services/FilterService`
- **Используется:** Компонентами поиска и фильтрации

### Особенности
- Извлечение данных из множества источников анализа
- Оптимизация производительности через useMemo
- Обработка различных форматов данных бэкенда
- Автоматическое обновление при изменении контекста
- Ограничение результатов для предотвращения лагов

**[⬆️ вернуться к модулям](#навигация-по-модулям)**

---

## Модуль `utils`

### Назначение
Вспомогательные утилиты и функции для преобразования данных, вычислений и работы с графиками. Инкапсулируют общую логику, не связанную с конкретными компонентами.

### Внутренняя структура
```
frontend/client/utils/
├── chartUtils.ts          # Утилиты для работы с диаграммами
└── dataTransform.ts       # Преобразование данных между форматами
```

### Основные компоненты

**chartUtils.ts**
- `calculateBarHeights()` - расчет высот элементов диаграмм
- `generateTicks()` - генерация значений для осей графиков
- Оптимизация расположения элементов визуализации

**dataTransform.ts**
- `transformRainbowData()` - преобразование данных цветовой классификации
- `transformTermsV2Data()` - преобразование данных сроков сопровождения
- Специализированные функции для искового/приказного производства
- Обработка несоответствий форматов бэкенда и фронтенда

### Ключевые функции

**chartUtils:**
- Автоматический расчет высот на основе количества элементов
- Генерация равномерных делений для осей графиков
- Учет отступов и пространства для подписей

**dataTransform:**
- Сопоставление технических имен с читаемыми русскими названиями
- Обработка неполных данных с заполнением нулями
- Преобразование между разными версиями API
- Интеграция с конфигурациями chartConfig

### Зависимости
- **Зависит от:** `config/chartConfig`
- **Используется:** Компонентами диаграмм и страницами анализа

### Особенности
- Обработка edge cases (несоответствие длин данных)
- Поддержка разных версий API (v1/v2)
- Типизированные преобразования для TypeScript
- Модульная архитектура с переиспользуемыми функциями
- Интеграция с системой маппинга конфигураций

**[⬆️ вернуться к модулям](#навигация-по-модулям)**

---

## Модуль `services`

### Назначение
Слой взаимодействия с бэкенд API. Инкапсулирует всю бизнес-логику работы с данными, предоставляя чистые интерфейсы для компонентов фронтенда.

### Внутренняя структура
```
frontend/client/services/
├── api/
│   ├── client.ts              # Базовый HTTP клиент
│   └── endpoints.ts           # Константы API endpoints
├── analysis/
│   ├── AnalysisService.ts     # Сервис управления анализом
│   ├── types.ts               # Типы данных анализа
│   └── index.ts               # Экспорт сервиса
├── case/
│   └── caseService.ts         # Сервис работы с делами
├── filter/
│   └── FilterService.ts       # Сервис фильтрации
└── saving/
    ├── SavingService.ts       # Сервис сохранения данных
    └── index.ts               # Экспорт сервиса
```

### Основные компоненты

**api/client.ts**
- Базовый HTTP клиент с методами GET/POST
- Обработка параметров запроса и ошибок
- Singleton паттерн для единого экземпляра

**api/endpoints.ts**
- Централизованное хранение всех API endpoints
- Группировка по функциональным модулям
- Поддержка версий API (v2)

**analysis/AnalysisService.ts**
- Управление последовательным и параллельным анализом
- Отслеживание прогресса выполнения
- Обработка ошибок и отмена задач
- Интеграция с AnalysisContext

**analysis/types.ts**
- Типизация всех данных анализа
- Интерфейсы для результатов, прогресса, ошибок
- Поддержка всех модулей системы

**case/caseService.ts**
- Получение детальной информации о делах
- Работа с документами дел
- Типизированные интерфейсы данных

**filter/FilterService.ts**
- Загрузка опций для фильтрации
- Применение фильтров к данным
- Метаданные доступных фильтров

**saving/SavingService.ts**
- Экспорт данных в различные форматы
- Управление статусом доступных данных
- Работа с blob для скачивания файлов

### Ключевые функции

**AnalysisService:**
- `runAnalysis()` - полный последовательный анализ
- `runSingleAnalysis()` - анализ конкретного модуля
- `cancelAnalysis()` - отмена выполнения
- `onProgress()` - подписка на обновления прогресса

**CaseService:**
- `getCaseDetails()` - детали дела по коду
- `getDocumentDetails()` - информация о документе

**FilterService:**
- `getFilterOptions()` - опции для полей фильтрации
- `applyFilters()` - применение фильтров к данным

**SavingService:**
- `saveData()` - экспорт данных в файл
- `getAvailableDataStatus()` - статус доступных данных

### Зависимости
- **Зависит от:** Бэкенд API endpoints
- **Используется:** Всеми компонентами и контекстами

### Особенности
- Единый HTTP клиент с обработкой ошибок
- Типизированные API responses
- Поддержка прогресса длительных операций
- Модульная архитектура сервисов
- Singleton паттерн для управления состоянием
- Централизованное управление endpoints
- Обработка различных форматов данных бэкенда

**[⬆️ вернуться к модулям](#навигация-по-модулям)**

---

## Модуль `components`

### Назначение
Переиспользуемые UI компоненты для построения интерфейса. Инкапсулируют визуальную логику и предоставляют единообразный внешний вид.

### Внутренняя структура
```
frontend/client/components/
├── TabsContainer.tsx              # Контейнер вкладок с навигацией
├── UploadFilesModal.tsx           # Модальное окно загрузки файлов
├── AnalysisProgress.tsx           # Индикатор прогресса анализа
├── TermsOfSupportMeanings.tsx     # Легенда цветовой системы сроков
├── Header.tsx                     # Верхняя панель с поиском и действиями
├── Layout.tsx                     # Основной макет приложения
├── DefaultChart.tsx               # Базовая диаграмма для визуализации
├── FieldGroup.tsx                 # Группа полей для отображения данных
├── NotificationModal.tsx          # Модальное окно уведомлений сотрудникам
├── PageContainer.tsx              # Контейнер страницы с отступами
├── ProgressBarModal.tsx           # Модальное окно прогресса анализа
├── RadioButtonGroup.tsx           # Группа радиокнопок для выбора
├── RainbowMeanings.tsx            # Легенда цветовой системы Rainbow
├── SavingModal.tsx                # Модальное окно выгрузки данных
├── SegmentedChart.tsx             # Сегментированная диаграмма для этапов
├── Settings.tsx                   # Компонент настроек фильтрации
└── Sidebar.tsx                    # Боковая панель навигации
```

### Основные компоненты

**TabsContainer.tsx** - Навигация по вкладкам с подсветкой активной.

**UploadFilesModal.tsx** - Загрузка обязательных и опциональных файлов с валидацией.

**AnalysisProgress.tsx** - Визуализация прогресса выполнения анализа.

**TermsOfSupportMeanings.tsx** - Цветовая легенда системы статусов сопровождения дел.

**Header.tsx** - Верхняя панель с поиском дел, загрузкой файлов и выгрузкой данных.

**Layout.tsx** - Основной макет приложения с боковой панелью и заголовком.

**DefaultChart.tsx** - Горизонтальная столбчатая диаграмма для визуализации данных.

**FieldGroup.tsx** - Группировка полей данных с адаптивным расположением колонок.

**NotificationModal.tsx** - Генерация и копирование уведомлений для сотрудников.

**PageContainer.tsx** - Базовый контейнер для страниц с едиными отступами.

**ProgressBarModal.tsx** - Отображение прогресса анализа с автоматическим закрытием.

**RadioButtonGroup.tsx** - Группа переключателей для выбора одного варианта.

**RainbowMeanings.tsx** - Легенда цветовой классификации дел системы Rainbow.

**SavingModal.tsx** - Выгрузка данных в различных форматах с проверкой доступности.

**SegmentedChart.tsx** - Сегментированная диаграмма для отображения этапов с разными статусами.

**Settings.tsx** - Компонент настройки фильтров с полями ввода и поиском.

**Sidebar.tsx** - Боковая панель навигации с отображением статуса загруженных файлов.

**/ui** - папка вспомогательных клмпонентов

### Зависимости
- **Зависит от:** `contexts/AnalysisContext`, `services/api`, `hooks`, `utils`, `components/ui`, `config/featureFlags`
- **Используется:** Страницами для построения интерфейса

### Особенности
- Единый дизайн-система с консистентными стилями
- Интеграция с контекстом анализа для управления состоянием
- Обработка пользовательского ввода и валидация
- Адаптивная верстка для разных устройств
- Поиск дел с автодополнением и навигацией
- Визуализация данных через кастомные диаграммы
- Группировка полей с типобезопасным форматированием
- Уведомления сотрудникам с копированием в буфер обмена
- Прогресс-бар с отслеживанием выполнения задач
- Цветовые легенды для систем Rainbow и Terms of Support
- Выгрузка данных с проверкой доступности и статуса
- Сегментированные диаграммы для многоуровневых данных
- Боковая панель с реальным статусом загруженных файлов


**[⬆️ вернуться к модулям](#навигация-по-модулям)**


